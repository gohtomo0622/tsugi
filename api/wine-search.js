export default async function handler(req, res) { res.setHeader("Access-Control-Allow-Origin", "*"); if (req.method === "OPTIONS") return res.status(200).end(); const APP_ID = (process.env.RAKUTEN_APP_ID || "").trim(); const ACCESS_KEY = (process.env.RAKUTEN_ACCESS_KEY || "").trim(); const old_url = "https://app.rakuten.co.jp/services/api/IchibaItem/Search/20170706?applicationId=" + APP_ID + "&keyword=wine&hits=3&formatVersion=2"; const new_url = "https://openapi.rakuten.co.jp/ichibams/api/IchibaItem/Search/20260401?applicationId=" + APP_ID + "&keyword=wine&hits=3&formatVersion=2"; try { const r1 = await fetch(old_url); const t1 = await r1.text(); const r2 = await fetch(new_url, { headers: { "Authorization": "Bearer " + ACCESS_KEY } }); const t2 = await r2.text(); return res.status(200).json({ old_status: r1.status, old: t1.slice(0,150), new_status: r2.status, new: t2.slice(0,150) }); } catch(err) { return res.status(500).json({ error: err.message }); } }