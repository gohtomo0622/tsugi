export default async function handler(req, res) { res.setHeader("Access-Control-Allow-Origin", "*"); if (req.method === "OPTIONS") return res.status(200).end(); const APP_ID = (process.env.RAKUTEN_APP_ID || "").trim(); const ACCESS_KEY = (process.env.RAKUTEN_ACCESS_KEY || "").trim(); const url = "https://openapi.rakuten.co.jp/ichibams/api/IchibaItem/Search/20260401?applicationId=" + APP_ID + "&accessKey=" + ACCESS_KEY + "&keyword=wine&hits=3&formatVersion=2"; try { const r = await fetch(url, { headers: { "Referer": "https://tsugi-two.vercel.app/", "Origin": "https://tsugi-two.vercel.app" } }); const t = await r.text(); return res.status(200).json({ status: r.status, result: t.slice(0,300) }); } catch(err) { return res.status(500).json({ error: err.message }); } }