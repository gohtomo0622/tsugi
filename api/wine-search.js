export default async function handler(req, res) { res.setHeader("Access-Control-Allow-Origin", "*"); if (req.method === "OPTIONS") return res.status(200).end(); const APP_ID = (process.env.RAKUTEN_APP_ID || "").trim(); const ACCESS_KEY = (process.env.RAKUTEN_ACCESS_KEY || "").trim(); const AFFILIATE_ID = (process.env.RAKUTEN_AFFILIATE_ID || "").trim(); const { tags, month } = req.query; const TAG_KEYWORDS = { earthy:"ナチュラルワイン 赤", funky:"自然派ワイン", mineral:"ナチュラルワイン 白", oxidative:"オレンジワイン", aged:"ナチュラルワイン 熟成", amber:"オレンジワイン", petnat:"ペットナット", effervescent:"ペットナット", fruity:"ナチュラルワイン", floral:"自然派ワイン 白", crisp:"ナチュラルワイン 白", bold:"ナチュラルワイン 赤", wild:"自然派ワイン", natural:"ナチュラルワイン", organic:"オーガニックワイン" }; const m = parseInt(month) || new Date().getMonth() + 1; let kw = "ナチュラルワイン"; if ([3,4,5].includes(m)) kw = "ナチュラルワイン 白"; if ([6,7,8].includes(m)) kw = "ペットナット 自然派"; if ([9,10,11].includes(m)) kw = "ナチュラルワイン 赤"; if ([12,1,2].includes(m)) kw = "オレンジワイン 自然派"; if (tags) { const t = tags.split(",")[0]; if (TAG_KEYWORDS[t]) kw = TAG_KEYWORDS[t]; } const params = new URLSearchParams({ applicationId: APP_ID, accessKey: ACCESS_KEY, keyword: kw, hits: "12", formatVersion: "2", availability: "1", imageFlag: "1" }); if (AFFILIATE_ID) params.set("affiliateId", AFFILIATE_ID); const url = "https://openapi.rakuten.co.jp/ichibams/api/IchibaItem/Search/20260401?" + params; try { const r = await fetch(url, { headers: { "Referer": "https://tsugi-two.vercel.app/", "Origin": "https://tsugi-two.vercel.app" } }); const text = await r.text(); if (!r.ok) { return res.status(200).json({ items: [], keyword: kw, error_detail: text.slice(0,200) }); } const data = JSON.parse(text); const items = (data.Items || []).slice(0,12).map(item => { const i = item.Item || item; const imgs = i.mediumImageUrls || i.smallImageUrls || []; const imgUrl = imgs.length > 0 ? (imgs[0].imageUrl || imgs[0]) : ""; return { name: i.itemName||"", price: i.itemPrice||0, priceStr: "¥"+(i.itemPrice||0).toLocaleString(), shop: i.shopName||"", imageUrl: imgUrl, itemUrl: i.affiliateUrl||i.itemUrl||"", reviewAvg: i.reviewAverage||0, reviewCount: i.reviewCount||0 }; }); return res.status(200).json({ items, keyword: kw, total: data.count||0 }); } catch(err) { return res.status(500).json({ error: err.message }); } }