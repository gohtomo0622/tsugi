export default async function handler(req, res) { res.setHeader("Access-Control-Allow-Origin", "*"); if (req.method === "OPTIONS") return res.status(200).end(); const APP_ID = (process.env.RAKUTEN_APP_ID || "").trim(); const ACCESS_KEY = (process.env.RAKUTEN_ACCESS_KEY || "").trim(); const url1 = "https://app.rakuten.co.jp/services/api/IchibaItem/Search/20170706?applicationId=" + APP_ID + "&keyword=wine&hits=3&formatVersion=2"; const url2 = "https://app.rakuten.co.jp/services/api/IchibaItem/Search/20170706?applicationId=" + APP_ID + "&accessKey=" + ACCESS_KEY + "&keyword=wine&hits=3&formatVersion=2"; try { const r1 = await fetch(url1); const t1 = await r1.text(); const r2 = await fetch(url2); const t2 = await r2.text(); return res.status(200).json({ test1_status: r1.status, test1: t1.slice(0,150), test2_status: r2.status, test2: t2.slice(0,150), app_id_len: APP_ID.length, key_len: ACCESS_KEY.length }); } catch(err) { return res.status(500).json({ error: err.message }); } }